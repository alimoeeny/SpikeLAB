function aliclicked(varargin)

switch_expr = [varargin{3}];

switch switch_expr
    case 'plotpsth'
        disp(strcat(varargin'));
    case ''
        disp(strcat(varargin'));
    otherwise
        disp(strcat(varargin'));
        
end
